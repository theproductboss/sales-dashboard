# Calendly (see README.md "Setup" section for how to get each of these)
CALENDLY_API_TOKEN=
CALENDLY_ORG_URI=
CALENDLY_OZ_USER_URI=
CALENDLY_BLAKE_USER_URI=

# Slack
SLACK_BOT_TOKEN=
CASH_REPORT_CHANNEL_ID=
DASHBOARD_CHANNEL_ID=

# Demio webinar alerts (see README.md "Webinar live attendee alerts")
DEMIO_API_KEY=
DEMIO_API_SECRET=
# Channel name (public, bot invited) or channel ID (works for private too)
WEBINAR_ALERTS_CHANNEL=#collective-webinars
# Minute marks (after webinar start) to post the alerts
WEBINAR_ALERT_MINUTES=2,15
# Only set if Demio session times come back shifted in dry-run logs
# DEMIO_TIMEZONE=America/New_York

# Report behavior
REPORT_TIMEZONE=America/New_York

# Targets (override defaults if goals change)
WEEKLY_REVENUE_TARGET=37000
MONTHLY_REVENUE_FLOOR=100000
OZ_WEEKLY_CALL_TARGET=15
BLAKE_WEEKLY_CALL_TARGET=20

# Set to "true" to print the report to the console instead of posting to Slack
DRY_RUN=false
